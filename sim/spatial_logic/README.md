# Spatial_Logic library

Name subject to change, but this library will concern how each vehicle is aware of the vehicles around itself.
Cars don't need to be aware of every vehicle in existence as that's n^2, so some logic will need to be implemented
to only have each vehicle be tracking the movements of as few other vehicles as possible.