# Diagnostics library

Here is where the logic governing our collection of data will live. Data will likely be compiled into either csv files
or raw binary, depending on space considerations. Either can be post processed by Python without issue.